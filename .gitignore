# Binaries
/agent-undo
/bin/
*.exe
*.test
*.out
coverage.out

# Go
vendor/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Contributor-only precaution:
# the Agent Undo store normally lives in ~/.agent-undo (or $AGENT_UNDO_HOME),
# not in the repository. Keep this ignored in case a contributor intentionally
# points AGENT_UNDO_HOME at the checkout.
.agent-undo/

# Local release dry-runs (official artifacts come from the tag workflow)
/dist/
/checksums.txt
/build-info.txt
/release-notes.md
